# Variables de entorno para PRODUCCIÓN
# Dominio: recetasdelmundo.site

# URL del API Backend (ruta relativa - nginx del contenedor maneja el proxy)
REACT_APP_API_URL=/api

# Entorno
REACT_APP_ENV=production

# Habilitar source maps (cambiar a false para ocultar código en producción)
GENERATE_SOURCEMAP=false
